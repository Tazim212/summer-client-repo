/* eslint-disable react/prop-types */
/* eslint-disable no-unused-vars */

const SectionTitle = ({heading}) => {
    return (
        <div>
            <h2 className="text-4xl font-bold text-gray-800 text-center my-7">{heading}</h2>
        </div>
    );
};

export default SectionTitle;